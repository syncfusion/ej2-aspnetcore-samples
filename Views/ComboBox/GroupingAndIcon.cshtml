@using Syncfusion.EJ2
@section ControlsSection{
    <div class="col-lg-12 control-section">
        <div id='groupList' class='col-lg-6' style='padding-top:15px'>
            <div class="content">
                <h4>Grouping</h4>
                <ejs-combobox id="vegetables" placeholder="Select a vegetable" popupHeight="200px" dataSource="@ViewBag.groupdata">
                    <e-combobox-fields groupBy="Category" text="Vegetable" value="Id"></e-combobox-fields>
                </ejs-combobox>
            </div>
        </div>
        <div id='iconList' class='col-lg-6' style='padding-top:15px'>
            <div class="content">
                <h4> Icons</h4>
                <ejs-combobox id="icons" dataSource="@ViewBag.icondata" placeholder="Select a social media">
                    <e-combobox-fields text="SocialMediaName" iconCss="Class" value="Id"></e-combobox-fields>
                </ejs-combobox>
            </div>
        </div>
    </div>
    @*custom code start*@
    <style>
    .content {
        margin: 0 auto;
        width: 250px;
    }
    </style>
    @*custom code end*@
    <link href="../css/dropdownlist/grouping.css" rel="stylesheet" />
}
@section Meta{
                    <meta name="description" content="This example demonstrates the Grouping and Icon in ASP.NET Core ComboBox control. Explore here for more details."/>
                }
@section ActionDescription{
    <div id="action-description">
        <p>This sample demonstrates the grouping and icons supports of the ComboBox. Type a character in the ComboBox element or click on the drodown icon to choose an item from the categorized list/icons list.</p>
    </div>
}
@section Description{
    <div id="description">
        <p>The ComboBox allows to group the relevant items under a corresponding category by mapping the <code>groupBy</code> field, and allows to load the list items with icons.</p>

        <p>The grouping sample illustrates how the vegetables are grouped based on its category.</p>

        <p>The 2nd ComboBox is populated with icons that is rendered by mapping the <code>iconCss</code> field.</p>
        <p>
            More information on the grouping feature configuration can be found in the
            <a href="https://ej2.syncfusion.com/aspnetcore/documentation/combo-box/grouping/" target="_blank"> documentation section</a>.
        </p>
    </div>
}
@section Title{
                 <title>ASP.NET Core ComboBox Grouping and Icon Example - Syncfusion Demos </title>
             }
@section Header{
                <h1 class='sb-sample-text'>Example for Grouping and Icon in ASP.NET Core ComboBox Control</h1>
            }