
@{
    ViewData["Title"] = "DefaultFunctionalities";
}

@using Syncfusion.EJ2

@section ControlsSection{
    <div class="col-lg-12 control-section toast-default-section">
        <div class="control_wrapper">
            <ejs-toast id="toast_default" title="Adaptive Tiles Meeting" content="Conference Room 01 / Building 135 10:00 AM-10:30 AM" icon="e-meeting" close="onclose" beforeOpen="onBeforeOpen">
                <e-toast-position X="Right"></e-toast-position>
            </ejs-toast>
            <div class="col-lg-12 col-sm-12 col-md-12 center">
                <div id="toastBtnDefault" style="margin: auto;text-align: center">
                    <button class="e-btn" id="toastBtnShow">Show Toasts</button>
                    <button class="e-btn" id="toastBtnHide">Hide All</button>
                </div>
            </div>
        </div>
    </div>

    <script type="text/javascript">

        var btnEleShow = document.getElementById('toastBtnShow');
        var btnEleHide = document.getElementById('toastBtnHide');

        document.onclick = function (e) {
            if (e.target !== btnEleShow) {
                var toastObj = document.getElementById('toast_default').ej2_instances[0];
                toastObj.hide('All');
            }
        };
        btnEleShow.onclick = function () {
            var toastObj = document.getElementById('toast_default').ej2_instances[0];
            toastObj.show();
        };

        btnEleHide.onclick = function () {
            var toastObj = document.getElementById('toast_default').ej2_instances[0];
            toastObj.hide('All');
        };

        function onclose(e) {
            if (e.toastContainer.childElementCount === 0) {
                btnEleHide.style.display = 'none';
            }
        }

        function onBeforeOpen() {
            btnEleHide.style.display = 'inline-block';
        }


        setTimeout(
            () => {
                var toastObj = document.getElementById('toast_default').ej2_instances[0];
                toastObj.show({
                    target: document.body
                });
            }, 500);
    </script>
    <style>
        #toast_default .e-meeting::before {
            content: "\e705";
            font-size: 17px;
        }
    </style>

}


@section ActionDescription{

    <div id="action-description">
        <p>This sample demonstrates the basic layout of a Toast to show simple notification and hide them.</p>
    </div>

}
@section Description{

  <div id="description">
    <p>The <code>Toast</code> is a notification pop-up used to display on the desired position with required message and header icons.</p>
    <ul>
        <li>The header text is set using <b>title</b> property.</li>
        <li>Information to be displayed is set using <b>content</b> property.</li>
    </ul>
    <p>
        More information about Toast can be found in this
        <a href="https://ej2.syncfusion.com/aspnetcore/documentation/toast/getting-started-asp-core/" target="_blank"> documentation section</a>.
    </p>

</div>

}

@section Meta{

    <meta name="description" content="The sample demonstrates about default functionalities of a Toast control and helps to design reminder like an alert in ASP.NET Core platform." />

}
