@using Syncfusion.EJ2.TreeMap;
@section ControlsSection{
    @{
        var levelBorder = new TreeMapBorder { Color = "black", Width = 0.5 };
    }
    <div class="control-section">
        <div class="row">
            <div class="col-md-12">
                <ejs-treemap id="container" load="treeMapLoad" dataSource="ViewBag.dataSource" weightValuePath="Gold">
                    <e-treemap-titlesettings text="US Gold medal categories in Summer Olympics - 2016"></e-treemap-titlesettings>
                    <e-treemap-leafitemsettings labelPath="Sport" fill="#993399" templatePosition="@Syncfusion.EJ2.TreeMap.LabelPosition.Center" border="levelBorder" labelFormat="${Sport} - ${Gold}"
                                                labelTemplate="<div><img src=../styles/images/treemap/{{:GameImage}} style=height:{{:ItemHeight}};width:{{:ItemWidth}};></img></div>">
                    </e-treemap-leafitemsettings>
                </ejs-treemap>
                <div style="float: right; margin-right: 10px;">
                    Source:
                    <a href=" https://en.wikipedia.org/wiki/United_States_at_the_2016_Summer_Olympics" target="_blank"> en.wikipedia.org</a>
                </div>
            </div>
        </div>
    </div>
}
@*custom code start*@
<script>
    function treeMapLoad(args) {
        args.treemap.leafItemSettings.showLabels = !ej.base.Browser.isDevice;
        var theme = location.hash.split('/')[1];
        theme = theme ? theme : 'Material';
        args.treemap.theme = theme.charAt(0).toUpperCase() + theme.slice(1);
    }
</script>
@*custom code end*@
@section Meta{
    <meta name="description" content="This demo for Essential JS2 TreeMap control depicts the gold medal categories of the 2016 US Summer Olympics. Each category is denoted with label template." />
}
@section ActionDescription{
    <div id="action-description">
        <p>
            This sample depicts the gold medal categories of US in 2016 summer Olympics. Each categories are denoted with label template.
        </p>
    </div>
}
@section Description{
    <div id="description">
        <p>
            In this example, you can see how to place custom HTML templates in the treemap items. Also we have formatted the labels default text.
        </p>
    </div>
}






